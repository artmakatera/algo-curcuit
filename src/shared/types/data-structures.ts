export type AdjacencyMatrix = number[][];

export interface VertexBaseData {
  id: number;    // Unique identifier that persists across operations
  value: string;  // Display value (A, B, C, etc.)
}



